# Docker image with Debian and cURL

This is a simple Docker image based on Debian `stable-slim`, with cURL, bzip2, and CA certificates installed.

The image name is `ghcr.io/maresb/docker-debian-curl`.
